import CommonImage from '@/components/common/image';
import Image from 'next/image';

interface Props {
  image: {
    url: string;
  };
}

export const BlockDetailImage = ({ image }: Props) => <></>;
